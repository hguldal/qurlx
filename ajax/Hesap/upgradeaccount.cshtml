@{
    
   if (!WebSecurity.IsAuthenticated)
   
        {
            Response.StatusCode=401;
            Response.End();
                   
        }  

     var db=Database.Open("u2p");
     var UserID=Request["UserID"];
     db.Execute("UPDATE Profiles SET HesapTuru=2 WHERE UserID=@0",UserID);
     db.Close();  
     Response.StatusCode=200;
     Response.End();
                
           
}

