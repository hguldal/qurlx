@{
     if (!WebSecurity.IsAuthenticated)
   
        {
            Response.StatusCode=401;
            Response.End();
                   
        }  

     var db=Database.Open("u2p");
 
     var kisaKod=Request["kisaKod"];
    
     var baYil= int.Parse(Request["baYil"]);
     var baAy= int.Parse(Request["baAy"]);
     var baGun= int.Parse(Request["baGun"]);

     var biYil= int.Parse(Request["biYil"]);
     var biAy= int.Parse(Request["biAy"]);
     var biGun= int.Parse(Request["biGun"]);

    DateTime baslangicTarihi=new DateTime(baYil,baAy,baGun);
    DateTime bitisTarihi=new DateTime(biYil,biAy,biGun);

    var sqlFormatliBaslangicTarihi = baslangicTarihi.ToString("yyyy-MM-dd HH:mm:ss");
    var sqlFormatliBitisTarihi = bitisTarihi.ToString("yyyy-MM-dd HH:mm:ss");

     var queryURL=db.QuerySingle("SELECT * FROM URL WHERE kisaKod=@0",kisaKod);

     if (db.QueryValue("SELECT COUNT(*) FROM UrlYonetici WHERE urlID=@0 AND userID=@1",queryURL.urlID,WebSecurity.CurrentUserId)<1)
     {
         db.Close();
         Response.StatusCode=401;
         Response.End();
     }


   
     var querySonuc=db.Query("SELECT country_name,count(*) as Visitor FROM Ziyaret WHERE urlID=@0 AND tarih BETWEEN @1 AND @2 GROUP BY country_name",queryURL.urlID,sqlFormatliBaslangicTarihi,sqlFormatliBitisTarihi);
    
    
     
    Json.Write(querySonuc, Response.Output);
    db.Close();   
    Response.ContentType = "application/json";
    Response.End();
 
}

