@{
   if (!WebSecurity.IsAuthenticated)
    {
        Response.StatusCode = 401;
        Response.End();
                   
    }
    
    var ID=Request["ID"];
    
    using (var db=Database.Open("veritabani"))
    {
  
        db.Execute("UPDATE Paylasim_Yorum SET Silindi=1 WHERE ID=@0",ID);
  
        db.Close();
   
        Response.Write("OK");
        Response.End();      
    }  
}

